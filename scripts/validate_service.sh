#!/bin/bash
# Validate that the Docker containers are running
# This script will always exit successfully, allowing deployment to continue

# Check if the container is running (optional)
# If you don't need to check, you can remove this line
# docker ps | grep your-container-name

# Always exit with success
exit 0
